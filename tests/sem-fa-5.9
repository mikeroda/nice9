#:compile
var x : int;
proc x(x, y : int)
  for x := x to y then
    for x := x/2 to (x-1)/2 then
      for x := ?(x < 1) to 0 then
        write x;
      end
      write x;
    end
    write x;
  end
  write x;
end
x(3, 1);
x(1, 3);
x(1, 1);
