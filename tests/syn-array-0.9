#:compile

type int10 = int[10];

proc sum(a : int10) : int
    sum := 0;
    for i := 0 to 9 then
        sum := sum + a[i];
    end
end

proc f() : int
    type ten = int[10];
    var a : ten;
    for i := 0 to 9 then
        a[i] = 17-i;
    end
    f := sum(a);
end

write f();
