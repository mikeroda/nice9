#:compile
type nil = int[0];
type nul = int[0];
forward f(x : nil) : int;
var a : nil;
proc f(x : nul) : int
  f := 1351;
  for i := 0 to -1 then
    f := f + x[i];
  end
end
for i := 0 to 20 then
  if i < 0 then
    a[i] := 9981*i;
  end
end
write f(a);
